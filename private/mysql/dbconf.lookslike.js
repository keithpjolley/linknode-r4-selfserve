'use strict';
  
const fs   = require('fs');
const path = require('path');

let local  = {
  linksprite_rw: {
    host      : 'localhost',
    user      : 'linksprite_rw',
    password  : '__mysqlpasswd__',
    database  : 'linksprite'
  }
};

